<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nich01as.kreader" >

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".KReaderApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.ExploreActivity"></activity>
        <activity android:name=".activity.TagsActivity"></activity>
        <activity android:name=".activity.PostButterActivity"></activity>
        <activity android:name=".activity.ButterListActivity"></activity>
        <activity android:name=".activity.ButterActivity"
            android:theme="@android:style/Theme.Material.Light.NoActionBar"
            ></activity>
    </application>

</manifest>
